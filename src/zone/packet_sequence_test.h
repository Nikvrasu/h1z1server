#ifndef PACKET_SEQUENCE_TEST_H
#define PACKET_SEQUENCE_TEST_H

#include <stdio.h>

// Simple test function to verify packet sequence tracking is working
static void test_packet_sequence_tracking() {
    printf("=== Packet Sequence Tracking Test ===\n");
    printf("This test verifies that packet sequence tracking is working correctly.\n");
    printf("Check console output for [PACKET TRACKING] messages.\n");
    printf("==================================\n");
}

#endif // PACKET_SEQUENCE_TEST_H